import React from "react";

const AppBar = () => (
  <header className="navbar-fixed">
    <nav>
      <div className="nav-wrapper">
        <a className="brand-logo center">React RxJS Chat App</a>
      </div>
    </nav>
  </header>
);

export default AppBar;
